# include <iostream>
# include "student.h"

//using namespace std ;

int main ()
{
	student ant[3];

	for (int i = 0 ; i <= 2 ; i++)
		s_tudent1(ant[i]);
	for (i = 0 ; i <= 2 ; i++)
		s_tudent2 (ant[i]);

	return 0;
}